name: pasar_lokal
description: Aplikasi Marketplace UMKM Lokal Indonesia
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.1
  shared_preferences: ^2.3.2
  image_picker: ^1.1.2
  cached_network_image: ^3.3.1
  intl: ^0.19.0
  shimmer: ^3.0.0
  badges: ^3.1.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
